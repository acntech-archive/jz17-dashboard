html
  include includes/head.pug
  body
    div(class='navbar navbar-inverse navbar-fixed-top')
      div(class='container-fluid')
        div(class='navbar-header')
          a(class='navbar-brand', href='/') Teammiljø Oversikt
          span(class='navbar-brand') -
          a(class='navbar-brand', href='/environment/' + environmentType + '/' + serverName + '/' + environmentName)= environmentName
    p(class='log-text')!= logs